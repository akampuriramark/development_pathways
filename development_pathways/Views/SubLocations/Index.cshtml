@model IEnumerable<development_pathways.Models.SubLocation>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h1 class="text-primary">List of Sub Locations</h1>
        </div>
        <div class="col-6 text-right">
            <a asp-action="Create" class="btn btn-primary">Create New Sub Location</a>
        </div>
    </div>
    @{
        if (Model.Count() > 0)
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SubLocationCode)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SubLocationName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.CreatedAt)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Location)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.SubLocationCode)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.SubLocationName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CreatedAt)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Location.LocationName)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.SubLocationId">Edit</a> |
                                <a asp-action="Details" asp-route-id="@item.SubLocationId">Details</a> |
                                <a asp-action="Delete" asp-route-id="@item.SubLocationId">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No Sub Location exists</p>
        }
    }
</div>
