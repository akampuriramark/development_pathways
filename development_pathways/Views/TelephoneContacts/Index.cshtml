@model IEnumerable<development_pathways.Models.TelephoneContact>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h1 class="text-primary">List of Telephone Contacts</h1>
        </div>
        <div class="col-6 text-right">
            <a asp-action="Create" class="btn btn-primary">Create New Telephone Contact</a>
        </div>
    </div>
    @{
        if (Model.Count() > 0)
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.TelephoneContact1)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.CreatedAt)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Application)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.TelephoneContact1)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CreatedAt)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Application.FullName)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.TelephoneContactId">Edit</a> |
                                <a asp-action="Details" asp-route-id="@item.TelephoneContactId">Details</a> |
                                <a asp-action="Delete" asp-route-id="@item.TelephoneContactId">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No Telephone Contact exists</p>
        }
    }
</div>