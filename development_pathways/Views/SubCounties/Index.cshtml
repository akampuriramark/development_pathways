@model IEnumerable<development_pathways.Models.SubCounty>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h1 class="text-primary">List of Sub Counties</h1>
        </div>
        <div class="col-6 text-right">
            <a asp-action="Create" class="btn btn-primary">Create New Sub County</a>
        </div>
    </div>
    @{
        if (Model.Count() > 0)
        {
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.SubCountyCode)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.SubCountyName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.CreatedAt)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.County)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.SubCountyCode)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.SubCountyName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.CreatedAt)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.County.CountyName)
                            </td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.SubCountyId">Edit</a> |
                                <a asp-action="Details" asp-route-id="@item.SubCountyId">Details</a> |
                                <a asp-action="Delete" asp-route-id="@item.SubCountyId">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No Sub County exists</p>
        }
    }
</div>